/* =====================================
 * Entry point
 * ===================================== */
import { attachUI } from './orchestrator.js';
window.addEventListener('DOMContentLoaded', ()=>{ attachUI(); });