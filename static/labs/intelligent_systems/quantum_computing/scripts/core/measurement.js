/**
 * Измерения квантового состояния: одного кубита, всего регистра, распределение вероятностей.
 */
export function measureAll(stateVector, collapse = true) {
  // TODO: вычислить распределение и выбрать случайное состояние (результат измерения всего регистра)
  const outcome = 0;
  return outcome;
}

export function measureOne(stateVector, qubitIndex, numQubits, collapse = true) {
  // TODO: вычислить вероятность 0/1 для указанного кубита и измерить его
  const outcome = 0;
  return outcome;
}

export function probabilityDistribution(stateVector) {
  // TODO: вернуть массив вероятностей для всех базисных состояний
  const probabilities = [];
  return probabilities;
}
